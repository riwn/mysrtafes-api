# See https://redocly.com/docs/cli/configuration/

apis:
  mysrtafes-api@1.0.0:
    root: ./documents/mysrtafes-api.yml

extends:
  - recommended

rules:
  # A sample custom rule. Read more about rules: https://redocly.com/docs/cli/rules/
  info-license: off

theme:
  openapi:
    generateCodeSamples:
      languages:
        - lang: curl
        - lang: Go
